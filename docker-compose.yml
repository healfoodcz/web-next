version: '3.7'

services:
  web-next:
    build:
      context: ./
      dockerfile: ./dockerfile
    container_name: web-next
    restart: always
    networks:
      - my-network

networks:
  my-network:
    external: true
